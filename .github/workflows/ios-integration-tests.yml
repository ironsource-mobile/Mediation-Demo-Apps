name: iOS Integration Tests

on:
  pull_request:
    paths:
      - 'iOS/**'
  workflow_dispatch:

jobs:
  integration-tests:
    runs-on: macos-26
    strategy:
      matrix:
        project:
          - name: Swift
            workspace: LevelPlayDemo.xcworkspace
            uses_cocoapods: true
          - name: Objective C
            workspace: LevelPlayDemo.xcworkspace
            uses_cocoapods: true

    defaults:
      run:
        working-directory: iOS/${{ matrix.project.name }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # IronSourceSDK 9.5.0.0 is built with the Xcode 26 toolchain. The macos-26
      # runner defaults to Xcode 26.5, which fails to link the Objective-C app
      # target against the Swift static pods (missing swiftCompatibility libs).
      # 26.6 links cleanly, so pin it explicitly.
      - name: Select Xcode 26.6
        run: sudo xcode-select -s /Applications/Xcode_26.6.app

      - name: Install CocoaPods dependencies
        if: matrix.project.uses_cocoapods
        run: pod install

      - name: Run integration tests
        run: |
          xcodebuild test \
            -workspace ${{ matrix.project.workspace }} \
            -scheme LevelPlayDemo \
            -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' \
            -resultBundlePath TestResults

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.project.name }}
          path: iOS/${{ matrix.project.name }}/TestResults
          retention-days: 7
